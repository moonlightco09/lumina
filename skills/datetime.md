# Date and Time Skill

When the user asks about the current date, time, day, or anything time-related:

1. Use the `run_command` tool with: `date`
2. Parse the output and answer naturally

Example: User asks "what day is it today?"
→ run_command: `date`
→ Reply: "Today is Monday, March 29, 2026"
